# Topic extraction

Topic extraction groups source sentences into named topics and nested topic paths. The topic-range input is bounded into LLM-sized chunks; responses are parsed, normalized, repaired for coverage, and combined back into article-level sentence ranges. There is no automatic refinement pass. Instead, the user can choose **Resplit** from a topic card's actions menu on the canvas. The same splitter runs again over only that card's sentence range, and the result replaces the card's topic and its subtopics within that range. Only summaries that covered the replaced sentences are regenerated.

The stage persists sentences and topics before summary generation begins. Parsed chunk results are also kept in a durable `topic_range_chunks` checkpoint, keyed to the record's content revision, so a resumed or retried run only re-requests the chunks it still needs. This checkpoint allows summary work to resume without recomputing the page analysis, while the topic hierarchy supplies the structure used by every view.

Entrypoint: [`src/core/pipeline/topicRangesStage.js`](../src/core/pipeline/topicRangesStage.js) (shared splitter: [`src/core/pipeline/topicRangeSplit.js`](../src/core/pipeline/topicRangeSplit.js); manual Resplit: [`src/core/pipeline/topicRangeResplit.js`](../src/core/pipeline/topicRangeResplit.js), [`src/core/pipeline/topicResplitApply.js`](../src/core/pipeline/topicResplitApply.js))
